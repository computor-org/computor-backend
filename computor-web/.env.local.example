# Local UI testing of the lecturer analytics dashboard against synthetic data,
# with no backend, DB, or snapshot required. Copy to .env.local and run
# `npm run dev`, then open /courses/<any-id>/lecturer/analytics.
NEXT_PUBLIC_ANALYTICS_DEMO=1
