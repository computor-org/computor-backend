# Coder Deployment Configuration
# Copy this file to .env and adjust values as needed

# Installation directory (where docker-compose.yml and templates are deployed)
CODER_DIR=/opt/coder

# Protocol for access URL (http or https)
CODER_PROTOCOL=http

# Coder domain (required - your server's domain or IP)
CODER_DOMAIN=coder.example.com

# Coder external port (exposed by Traefik)
CODER_PORT=8443

# Protocol for access URL (http or https)
CODER_PROTOCOL=https

# PostgreSQL configuration
CODER_POSTGRES_PORT=5439
CODER_POSTGRES_USER=coder
# Leave empty to auto-generate a secure password
CODER_POSTGRES_PASSWORD=

# Docker group ID (auto-detected if not set)
# Run: getent group docker | cut -d: -f3
DOCKER_GID=

# Admin user credentials (optional)
# If not set, first user to sign up becomes admin
CODER_ADMIN_USERNAME=admin
CODER_ADMIN_EMAIL=admin@example.com
# Leave empty to be prompted during install
CODER_ADMIN_PASSWORD=

# API secret for Traefik protection (auto-generated if not set)
# Pre-generate with: openssl rand -hex 32
# Share this with computor-backend for user/workspace creation
CODER_ADMIN_API_SECRET=
