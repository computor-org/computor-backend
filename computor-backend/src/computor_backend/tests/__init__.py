"""
Test package for computor_backend.

Run via ``./computor.sh test`` from the repo root (``--unit`` needs no database,
``--integration`` expects the dev stack up). Shared fixtures live in conftest.py.
"""