import numpy as np

a = 5.4
b = 1.2

sum_ab = a + b
diff = a - b
prod = a * b
quot = a / b
pot = a**b

wurzel = np.sqrt(a)
root3 = a ** (1 / 3)
expo = np.exp(-a)

trig1 = np.sin(a)
trig2 = np.sin(a + b)
trig3 = np.cos(np.pi * a)
